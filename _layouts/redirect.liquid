<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="refresh" content="0; url={{ page.redirect | relative_url }}">
    <link rel="canonical" href="{{ page.redirect | relative_url }}">
    <title>Redirecting…</title>
  </head>
  <body>
    <p>This page has moved to <a href="{{ page.redirect | relative_url }}">{{ page.redirect }}</a>.</p>
  </body>
</html>
